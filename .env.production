# Public RevenueCat Web Billing SDK key (safe to ship in the client bundle).
# Kept here so Vercel production builds always receive it even if the
# dashboard env var is missing/misnamed. Override via Vercel env if needed.
VITE_REVENUECAT_PUBLIC_KEY=rcb_TdRZaSXDttTeYETrokkCKpvLqdYQ
